global:
  imagePullSecrets:
    - name: dockerhub-pull

tls:
  enabled: false
  autoGenerated: false

replicaCount: 4

ui:
  color: "#2ecc71"
  message: "Hello from ArgoCD - PROD"

service:
  type: ClusterIP
  port: 9898

resources:
  requests:
    cpu: 60m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
